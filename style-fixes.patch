--- a/css/style.css
+++ b/css/style.css
@@ -869,7 +869,7 @@ body{font-family:var(--font);background:var(--chat-bg);color:var(--t1);font-siz
 .mbody{background:var(--msg-bg);border:1px solid var(--b);border-radius:18px;padding:6px 12px 7px;position:relative;word-break:break-word;transition:background .12s,border-color .12s;min-width:0;max-width:100%;box-sizing:border-box}
-.me .mbody{background:var(--msg-bg);border:none;border-radius:18px;color:#fff;overflow:hidden;isolation:isolate}
-.me .mbody::before{content:'';position:absolute;inset:0;border-radius:inherit;background:var(--msg-me-bg, linear-gradient(to bottom, var(--y2), var(--y)));background-attachment:fixed;pointer-events:none;z-index:-1}
+.me .mbody{background:transparent;border:none;border-radius:18px;color:#fff;overflow:hidden;position:relative}
+.me .mbody::before{content:'';position:absolute;inset:0;border-radius:inherit;background:var(--msg-me-bg, linear-gradient(180deg, var(--y2) 0%, var(--y) 100%));background-attachment:fixed;pointer-events:none;z-index:0}
+@supports not (background-attachment:fixed){.me .mbody::before{background-attachment:scroll;background-size:100% 100vh;background-position:0 0}}
 .mbody > *{position:relative;z-index:1}
@@ -954,7 +954,7 @@ body{font-family:var(--font);background:var(--chat-bg);color:var(--t1);font-siz
-.me .mbody.media-only{background:var(--msg-bg);border:none;overflow:hidden;isolation:isolate}
+.me .mbody.media-only{background:transparent;border:none;overflow:hidden;position:relative}
 .me .mbody.media-only::before{content:'';position:absolute;inset:0;border-radius:inherit;background:var(--msg-me-bg, linear-gradient(to bottom, var(--y2), var(--y)));background-attachment:fixed;pointer-events:none;z-index:-1}
@@ -976,7 +976,7 @@ body{font-family:var(--font);background:var(--chat-bg);color:var(--t1);font-siz
-.me .mbody.has-media-caption{background:var(--msg-bg);border:none;overflow:hidden;isolation:isolate}
+.me .mbody.has-media-caption{background:transparent;border:none;overflow:hidden;position:relative}
 .me .mbody.has-media-caption::before{content:'';position:absolute;inset:0;border-radius:inherit;background:var(--msg-me-bg, linear-gradient(to bottom, var(--y2), var(--y)));background-attachment:fixed;pointer-events:none;z-index:-1}
@@ -1198,12 +1198,17 @@ body{font-family:var(--font);background:var(--chat-bg);color:var(--t1);font-siz
 #file-in{display:none}
-.attach-btn{
-  width:44px;height:44px;flex-shrink:0;
-  background:var(--chat-btn-bg, var(--solid2));
-  border:1px solid var(--chat-btn-b, var(--b2));
-  border-radius:50%;
-  cursor:pointer;
-  display:flex;align-items:center;justify-content:center;
-  color:var(--t1);
-  transition:background .15s,color .15s,transform .12s var(--sp),border-color .15s;
-}
-.attach-btn:hover{
-  background:var(--solid3);
-  color:var(--t1);
-  border-color:var(--b3);
-}
-.attach-btn:active{transform:scale(.88)}
-.attach-btn svg{width:18px;height:18px}
+.attach-btn{
+  position:absolute;left:9px;bottom:9px;z-index:3;
+  width:26px;height:26px;flex-shrink:0;
+  background:var(--chat-btn-bg, rgba(255,255,255,.08));
+  border:1px solid var(--chat-btn-b, transparent);
+  border-radius:50%;
+  cursor:pointer;
+  display:flex;align-items:center;justify-content:center;
+  color:rgba(255,255,255,.7);
+  transition:color .15s,background .15s,transform .12s var(--sp);
+}
+.attach-btn:hover{background:rgba(255,255,255,.14);color:#fff;transform:scale(1.08)}
+.attach-btn:active{transform:scale(.88)}
+.attach-btn svg{width:16px;height:16px}
+.input-row{position:relative}
+.mfield{padding:11px 42px 11px 42px}
@@ -1558,6 +1563,22 @@ body[data-theme="light"] .pm-danger-btn:hover{background:rgba(255,69,58,.07)}
 body[data-theme="light"] .mtxt .spoiler.revealed{
   background:rgba(139,92,246,.18);
   text-shadow:none;
 }
+
+/* bug3: attach btn in light theme */
+body[data-theme="light"] .attach-btn{
+  background:rgba(0,0,0,.1);
+  border-color:rgba(0,0,0,.12);
+  color:rgba(0,0,0,.55);
+}
+body[data-theme="light"] .attach-btn:hover{background:rgba(0,0,0,.17);color:rgba(0,0,0,.8)}
+
+/* bug4: emoji picker button in light theme */
+body[data-theme="light"] .emo-btn-in{
+  background:rgba(0,0,0,.1);
+  border-color:rgba(0,0,0,.12);
+  color:rgba(0,0,0,.55);
+}
+body[data-theme="light"] .emo-btn-in:hover{background:rgba(0,0,0,.17);color:rgba(0,0,0,.8);transform:scale(1.08)}
+body[data-theme="light"] .emo-btn-in.active{background:var(--ybg);border-color:var(--yb);color:var(--y)}
